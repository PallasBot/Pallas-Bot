# 仅供 Docker Compose 插值（如 postgres 服务的 POSTGRES_*），与 config/pallas.toml 中 [bootstrap.postgres] 保持一致。
# 复制为 pallas-bot/config/compose.env 后：
#   docker compose --env-file ./pallas-bot/config/compose.env up -d
# 3.x 沿用 MongoDB：追加 --profile mongo
PG_USER=pallas
PG_PASSWORD=pallas
PG_DB=PallasBot
# Compose 镜像地址（可选；不填时使用 docker-compose.yml 中的官方默认值）
# 可填写私有 Registry 或镜像代理提供的完整地址；修改后用 docker compose config --images 检查。
# PALLAS_BOT_IMAGE=pallasbot/pallas-bot:latest
# POSTGRES_IMAGE=postgres:16-alpine
# MONGO_IMAGE=mongo:8.0.10-noble
# REDIS_IMAGE=redis:7-alpine
# OLLAMA_IMAGE=ollama/ollama:latest
# Bot / work 容器运行时使用的 uv Python 包索引（插件安装等）；默认 PyPI。
# UV_DEFAULT_INDEX=https://pypi.org/simple
# 端口（全栈 compose，见文档站 Docker 部署「全栈」）：
#   BOT_PORT         宿主机访问 Bot（浏览器），默认 8088
#   BOT_LISTEN_PORT  容器内 Bot 监听端口，默认 8088；AI 回调用同一值，勿与 BOT_PORT 混用
# BOT_PORT=8088
# BOT_LISTEN_PORT=8088
# AI 镜像：默认 slim（LLM-only）；GPU 唱歌/TTS 用 latest
# PALLAS_AI_IMAGE=pallasbot/pallas-bot-ai:slim
# 预拉 Ollama 模型（可选）: docker compose ... --profile pull-models up -d
# 否则在 WebUI「AI 配置 → 接入」勾选「切换时拉取」
# Bot WebUI「AI 服务」Bearer Token 须与下方一致（注入 pallasbot-ai 的 PALLAS_AI_API_TOKEN）
# PALLAS_AI_API_TOKEN=
