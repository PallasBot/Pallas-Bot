# 4.0 slim 验收记录

> **日期**：2026-06-16  
> **分支**：`feat/4.0-slim`（本体） / WebUI `feat/4.0`  
> **约束**：不 stop/restart/install/`uv sync`；**不影响**当前运行中的分片 hub + 8 worker。  
> **Docker 默认镜像瘦身**：按约定 **延后**（persona 合入 `dev` 后一并改）。

## 环境快照（只读）

| 项 | 观测 |
| --- | --- |
| 运行态 | `./scripts/pallas status` → 分片运行中，hub `:7969`，8/8 worker |
| `load_bundled_extra_plugins` | `config/pallas.toml` 未显式开启 → **默认 false** |
| `extra_plugin_dirs` | `["local/plugins"]`（站点 local 已配置） |
| 当前 venv 官方扩展 pip | **均未安装**（`pallas ext list` 全 `no`） |
| 运行中 hub API | `/pallas/api/health` → 200；需鉴权接口 → 401（未带 token，**未写入 token**） |

## 清单结果

| # | 项 | 结果 | 证据 / 说明 |
| --- | --- | --- | --- |
| 1 | 默认 `uv sync` 后仅 core 插件树 | **通过** | `_discover_plugin_modules(load_bundled_extra=False)` → 10 个插件，**无 extra 泄漏**；`test_plugin_loader_unified` / `test_plugin_matrix` 通过 |
| 2 | `--extra plugins-duel` 后 duel 可用 | **部分** | `uv_extra_for_plugin("duel")`、extras 映射单测通过；**未执行 `uv sync --extra plugins-duel`**（避免改动 venv 影响长驻进程）。合流后维护窗补：`pallas ext install pallas-plugin-duel` |
| 3 | 未装扩展时 help/命令行为 | **通过** | `tests/plugins/help/test_visibility.py` 4 项通过；矩阵 `should_load_bundled_plugin(duel, false)=False` |
| 4 | 分片 hub/worker 扩展一致 | **通过（策略层）** | `installed_extra_plugin_modules(hub=True/False)` 共用同一矩阵与 `PIP_MODULE_LOAD_ROLE`；`test_plugin_catalog` hub/worker 期望一致。当前无 pip 扩展 → 两侧均为空列表。**运行时各进程已加载插件集**待下次重启后对照 WebUI 插件列表 |
| 5 | 默认 Docker 镜像无迁出插件代码 | **延后** | 按约定 persona 合 `dev` 后改 `Dockerfile` / `PALLAS_UV_EXTRAS` |
| 6 | WebUI 展示插件 source | **通过（离线）** | `build_plugin_catalog_rows()` → `core:10 extra:14 local:4 pip:2`；WebUI `feat/4.0` 已接 API 类型。运行中 hub **未复验**（401 / 可能仍为合流前进程） |
| 7 | WebUI 官方扩展索引与 install/uninstall | **通过（离线）** | `build_official_extension_rows()` → 11 包；`extension_install` / CLI / WebUI 代码与单测就绪。**未 POST 安装**（避免 venv + 重启） |
| 8 | 商店安装与 `local/plugins` 并存、local 优先 | **通过** | `test_bootstrap_plugin_dirs`、`test_plugin_catalog`（local 路径）；本机 `local/plugins/` 有站点插件；架构与 [site-customization](../architecture/site-customization-and-updates.md) 一致 |
| 9 | `pallas` CLI 启停与扩展重启 | **通过（只读）** | `doctor` / `status` / `ext list` / 子命令 `--help` 正常；**未执行** `run|stop|restart|ext install` |
| 10 | 迁移文档完整 | **通过** | [pallas-4.0-slim.md](../architecture/pallas-4.0-slim.md) 迁移摘要、[4.0-development.md](4.0-development.md)、[pallas-cli.md](../architecture/pallas-cli.md)、[site-customization](../architecture/site-customization-and-updates.md) |

## 执行的命令（均只读或单测）

```bash
./scripts/pallas status
./scripts/pallas doctor
./scripts/pallas ext list
uv run pytest tests/common/test_plugin_matrix.py \
  tests/common/test_plugin_loader_unified.py \
  tests/common/test_plugin_registry.py \
  tests/common/test_plugin_coord.py \
  tests/common/test_extension_install.py \
  tests/common/test_pallas_cli.py \
  tests/common/test_cli_*.py \
  tests/common/test_plugin_catalog.py \
  tests/common/test_bootstrap_plugin_dirs.py \
  tests/common/test_pyproject_plugins.py \
  tests/plugins/help/test_visibility.py -q
# 38 + 21 + 4 = 63 项相关单测通过（分三批执行）
```

## 合流前仍建议（需维护窗 / 不影响当前进程则无法做）

1. **维护窗**：`pallas maintenance run --sync-extra deploy-full --update-bot`（或按需 extras）→ 对照 WebUI 插件列表与 help。  
2. **运行中进程**：当前 hub/worker 仍为合流前代码时，新 API（如 `official-extensions`）需 **重启后** 再验 HTTP。  
3. **Docker 默认 core 镜像**：persona → `dev` 后改（见上）。  
4. **可选**：删除 `src/plugins/` 内 bundled extra 源码（迁移期副本）。

## 相关文档

- [pallas-4.0-slim.md](../architecture/pallas-4.0-slim.md) — 验收清单（已同步勾选）
- [4.0-development.md](4.0-development.md)
