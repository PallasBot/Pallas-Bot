# {展示名} `{包名}`

{一两句：插件做什么，以及什么时候需要它。句号结尾。}

**类型**：{本体 core（默认加载） / 官方插件（需安装） / 随主仓提供}

`PluginMetadata` 文案格式见 [cmd_perm · 写法约定](../common/cmd_perm/README.md) 与 `metadata_text.py`（`usage_line` + `join_usage` 自动编号、`SCENE_*` 等）。面向用户的 `usage` / `trigger_condition` **不要写死**「仅群管」「默认群主」等角色名；「何人可用」由帮助图按命令权限与 WebUI 覆盖展示。

## 安装

{核心：默认加载，无需单独安装。}
{官方：控制台插件商店，或 `uv run pallas ext install pallas-plugin-...`。}
{已内核化：随主仓提供，无独立安装步骤。}

## 用法

| 命令 / 触发 | 场景 | 说明 |
| --- | --- | --- |
| … | 群内 / 私聊 / 自动 | … |

精确命令、限制与「何人可用」以群内 **牛牛帮助** 为准。无群内命令的插件可删本表，改写一句说明。

## 命令权限（代码默认）

| 命令 ID | 默认等级 |
| --- | --- |
| `{包名}.…` | 所有人 / 群管或号主 / … |

实际生效等级以控制台「命令权限」或环境变量覆盖为准。无独立命令权限的被动功能可删本节。

## 配置

可在控制台对应插件页（或文中写明的 **AI 配置** / 通用配置段）修改。保存后写入 `data/pallas_config/webui.json`。

| 键 | 默认 | 说明 |
| --- | --- | --- |
| … | … | … |

字段以 [`packages/{包名}/config.py`](../../../packages/{包名}/config.py) 为准（**无 `config.py` 则删表**，改为说明配置入口）。官方扩展写扩展仓路径。本模板假定正文位于 `docs/plugins/<name>/README.md`。

## 排障

| 现象 | 处理 |
| --- | --- |
| … | … |

无可写排障时可删本节。

## 源码

- 主仓：[`packages/{包名}/`](../../../packages/{包名}/)
- 官方扩展：扩展仓对应目录，例如 `src/pallas_plugin_xxx/`

按需列出关键文件与 2～4 条实现边界；不要堆大段实现细节。

## 相关链接

- [插件文档索引](../README.md)
- [命令权限说明](../common/cmd_perm/README.md)
- {相关通用能力 / 相关插件（按需）}
